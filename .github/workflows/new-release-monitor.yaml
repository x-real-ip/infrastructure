name: Check releases

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-releases:
    uses: x-real-ip/github-actions/.github/workflows/new-release-issue.yaml@main
    strategy:
      matrix:
        include:
          - name: Ingress NGINX Controller
            target_repo: kubernetes/ingress-nginx
          - name: Immich
            target_repo: immich-app/immich
          - name: DSMR-Reader
            target_repo: xirixiz/dsmr-reader-docker
    with:
      name: ${{ matrix.name }}
      target_repo: ${{ matrix.target_repo }}
