# Sealed Secrets name
sealed_secrets_manual_name: "sealed-secrets"
# Sealed Secrets namespace
sealed_secrets_manual_namespace: "kube-system"
# Sealed Secrets container image tag
sealed_secrets_manual_image_tag: "v0.23.1"
